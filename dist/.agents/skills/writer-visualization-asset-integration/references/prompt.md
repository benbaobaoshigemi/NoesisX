# Injected Prompt Seed

Source basis:

- `../../noesisx-push-writer/references/visualization-asset-integration.md`

Inject these rules into the writer brief:

- 若文稿需要图文并茂，优先使用 `visualization/` 中已经存在的图片。
- 不得虚构并未实际存在的图。
- 图必须服务论证，不是装饰。
- 图题、编号、正文中的引用位置必须一致。
- 图片尺寸与放置位置不得破坏整体版式。

Output expectation:

- 返回图像接入方案，或直接在成稿要求中明确图像放置与引用规则
